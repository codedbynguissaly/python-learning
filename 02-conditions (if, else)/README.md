# 🔀 Conditions (if, else)

## 📖 What are conditional statements?

Conditional statements allow a program to make decisions based on whether a condition is **True** or **False**.

The two main keywords are:

- **if** → Executes a block of code if the condition is true.
- **else** → Executes a block of code if the condition is false.

---

## 📝 Syntax

```python
if condition:
    # code if the condition is true
else:
    # code if the condition is false
```

---

## 💡 Example

```python
age = 18

if age >= 18:
    print("You are an adult.")
else:
    print("You are a minor.")
```

---

## 🔍 Comparison Operators

| Operator | Meaning |
|----------|---------|
| `==` | Equal to |
| `!=` | Not equal to |
| `>` | Greater than |
| `<` | Less than |
| `>=` | Greater than or equal to |
| `<=` | Less than or equal to |

---

## 🎯 What I Learned

- How to write an `if` statement.
- How to use `else`.
- How to compare values.
- How Python makes decisions based on conditions.

---

## 📂 Files

- `conditions.py` → Examples and exercises using `if` and `else`.

---

⭐ This lesson is part of my Python learning journey.